const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const deals = await (prisma.pipeline_deals || prisma['pipeline_deals']).findMany(); const withPlanner = deals.filter(d => d.sales_planner && d.sales_planner.trim() !== ''); console.log('Total deals:', deals.length, 'With planner:', withPlanner.length); console.log(withPlanner.map(d => ({id: d.id, sp: d.sales_planner, status: d.status, source: d.source, closed: d.is_closed}))); } main();

const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const pics = await prisma.pipeline_settings.findUnique({where: {key: 'PARTNERSHIP_PICS'}}); console.log('PARTNERSHIP_PICS:', pics); const deals = await prisma.pipeline_deals.findMany({where: {sales_planner: {not: null}}, select: {sales_planner: true}}); console.log('Unique sales_planners:', [...new Set(deals.map(d => d.sales_planner))]); } main().finally(() => prisma.$disconnect());

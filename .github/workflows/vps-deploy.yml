name: VPS Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /var/www/daikin-connect
            git pull origin main
            npm install
            npm run build
            pm2 restart daikin-connect || pm2 start npm --name "daikin-connect" -- start
            pm2 save

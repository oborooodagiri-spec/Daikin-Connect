import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const deals = await prisma.pipeline_deals.findMany({ where: { sales_planner: { not: null, not: '' } }, select: { sales_planner: true, status: true, is_closed: true, created_at: true } }); console.log('Found:', deals.length, 'deals'); console.log(deals.slice(0, 5)); } main().finally(() => prisma.$disconnect());
